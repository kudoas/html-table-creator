name: ci
on: pull_request
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: 18.20.8
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm build
      - run: pnpm test
